'use strict';

const { twilio } = require('./handler');

test('the commands command', async () => {
  // const event = { body: "From=447375331944&Body=COMMANDS" };
  // const response = await twilio(event);
  // expect(response.body).toMatchSnapshot();
  // expect(response.headers["Content-Type"]).toEqual("text/xml");
});
